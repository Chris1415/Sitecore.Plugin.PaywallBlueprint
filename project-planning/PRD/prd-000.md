# Product Requirements Document

---
document_type: prd
prd_id: PRD-000
artifact_name: prd-000.md
generated_at: 2026-05-13T09:34:04Z
run_manifest: project-planning/workflow/run-20260513T093404Z.json
source_inputs:
  - project-planning/brain-dumps/brain-dump-20260513T093404Z.md
  - /create-prd discovery session 2026-05-13 (DQ1–DQ6)
consumed_by:
  - Software Architect Agent
next_input:
  - project-planning/architecture/architecture-20260513T093404Z.md
---

<!-- PRD-000 = holistic / greenfield foundation for the paywall-blueprint product. Subsequent feature PRDs will be prd-001 (real provider), prd-002 (seat management), prd-003 (self-service customer portal). -->

## 1. Executive Summary

**Paywall Blueprint** is a public OSS reference Sitecore Marketplace App that demonstrates how to integrate a paywall — tenant entitlement check, self-service purchase, license sync — into a Marketplace App end-to-end. It is the **first publicly available worked example** of monetizing a Sitecore Marketplace App. The reference app itself has no real feature behind the paywall; the paywall flow IS the feature. The deliverable is an installable Cloud Portal Marketplace App on the **SitecoreAI Full Screen** extension point that demonstrates a freemium pattern (free piece + gated premium piece on one page) plus the full **denial-state UX library** (4 design-reference state components), all driven by a `<PaywallGate>` React wrapper and an entitlement store. Two clean abstraction boundaries — a `PaymentProvider` adapter (**Stripe direct** in v1 — Stripe Billing + Entitlements API + Customer Portal; swappable to Polar.sh / Lemon Squeezy via single-file adapters) and an `EntitlementStore` adapter (Supabase Postgres in v1, swappable to Upstash / Firestore / etc.) — let adopters fork the repo, replace placeholder concerns, and ship a monetized Marketplace App without reinventing the gating pattern.

PRD-000 delivers the **foundation tranche** of a four-PRD product. It establishes the gate component, the entitlement-store abstraction, the freemium UX shell, all four denial-state UX components (the latter two as design-reference components — see § 5), the env-flag toggle that switches the gate between enforced and demo modes, and the OSS launch surface (public GitHub repo + README). Entitlement evaluation in PRD-000 is **tenant-only** (allowed or no-subscription) — per-user seat enforcement (counting, capacity check, unassigned detection) is deferred to PRD-002. PRD-001 adds the real Stripe provider + Checkout + webhook handler + Entitlements sync. PRD-002 adds per-user seat enforcement + admin invite/revoke. PRD-003 adds the self-service customer portal via Stripe Customer Portal (one API call; Stripe hosts the entire UI for card / plan / cancel / invoices). The blueprint is architected as public-app-ready from day one (Blok compliance, accessibility) but registered as a **custom app** for PRD-000 ship; submission to the public Sitecore Marketplace listing is deferred to a post-PRD-003 stretch.

Provider research is at `storage/paywall-providers-research-2026-05-13.md` (§ 8 has the wiring diagram, 4 user flows, webhook event table, and client-gate-vs-server-gate security note that adopters must respect).

## 2. Problem Statement

Sitecore's Cloud Portal Marketplace today provides **no built-in commerce primitives**. Every team that wants to charge for a Marketplace App — whether freemium or fully paid — starts from zero on every decision: which payment provider, how to map Cloud Portal tenants to subscription objects, how to enforce seats inside an iframe sandbox, what to show editors when access is denied, how to handle reinstalls, how to wire webhooks back to entitlements, what to do when the payments provider is down. The result is that the Marketplace ecosystem stays effectively **free-only** because the per-app cost of inventing a paywall in isolation is too high.

A vendor-neutral, opinionated, public worked example removes that barrier. With a forkable reference repo and a documented swap-point pattern, adopters move from "I'd have to invent a paywall from scratch" to "I fork this repo and replace one component." This unlocks the first wave of paid / freemium Marketplace Apps that aren't viable today.

## 3. Goals and Success Metrics

**Product goal:** Become the de-facto public reference for monetizing a Sitecore Marketplace App. Adopters who want a paywall should find this blueprint before they consider rolling their own.

**PRD-000 ship goals (required for "shipped" status):**

| Goal | Type | Acceptance |
|------|------|-----------|
| **G1 — Functional smoke pass on real tenant** | Functional / mandatory | All four `<PaywallGate>` UX states render correctly when entitlement-store rows are flipped via the state-switcher CLI. Env-flag toggle visibly engages the demo-mode banner. Post-gate welcome screen renders user + tenant from `application.context`. Screenshot evidence captured for each state. |
| **G2 — OSS launch artifact ready** | Asset / mandatory | Public GitHub repo set to **public visibility** (the ship moment). README explains the pattern, the two adapter interfaces, the env-flag, and the swap-points. CHANGELOG records PRD-000 release. Code organized so the abstraction boundaries are visible at a glance. |
| **G3 — Cold-read README check** | Functional / mandatory | A named cold-reader (operator picks: a colleague, a contractor, an LLM agent with clean context) reads only the README. Outcome recorded as a one-paragraph note on `docs/cold-read-notes.md`: could they understand the pattern, identify the two swap-points, and describe what they would change in their own app? Pass = yes to all three. **G3 is a launch gate.** |

**Ship moment definition:** PRD-000 is "shipped" when (i) G1 screenshots are committed and visible in the README or `docs/smoke-walkthrough.md`, (ii) the GitHub repo is flipped from private to public visibility, and (iii) G3's cold-read note is committed. All three required.

**Deferred to PRD-001 ship:** Walkthrough video — a video walkthrough is more compelling once a real payment flow can be shown end-to-end. Recording it in PRD-000 (with placeholder URLs) would risk a re-shoot.

**Discovery surface — out of scope for PRD-000:** No launch announcement, blog post, hahnsolo.net page, or Marketplace public-listing submission in this PRD. The repo flipping public *is* the artifact; promotion happens (or doesn't) post-PRD-001 when the walkthrough video gives the launch announcement something to point at.

**Vanity metrics (intentionally not tracked):** GitHub stars, forks, downloads. The blueprint succeeds by being the *correct* answer for adopters who find it, not by being widely cloned.

## 4. Target Users

### Primary — Marketplace App developers (direct consumers of the blueprint)

- **Persona:** Independent vendors, agencies, partner studios, or in-house teams who have built or are planning a Marketplace App and want to monetize it.
- **What they consume:** The public GitHub repo, the README explaining the pattern, the two adapter interfaces, the reference app's source structure.
- **How they use it (primary path — README optimizes for this):** **Fork the whole repo**, rename it, replace the placeholder free + premium sections with their own content, swap the provider / store adapter if needed, redeploy.
- **How they use it (secondary path — documented as advanced):** **Copy `src/lib/paywall/`** into an existing Next.js Marketplace app. More work; recommended only for adopters with an existing app they don't want to fork.
- **Success for them:** They ship a monetized Marketplace App without inventing the gating pattern from scratch.

### Primary — hahnsolo's own future Marketplace Apps

- **Persona:** hahnsolo product team (you, future maintainers).
- **What they consume:** The same OSS blueprint. The reference implementation is the reusable foundation for any future hahnsolo app that wants commercial gating (e.g., Component Usage Atlas Pro, Redirect Manager Pro).
- **Success for them:** Each new hahnsolo Marketplace App can adopt the gating pattern in hours, not days.

### Secondary — Editors and tenant admins on a paywalled tenant (UX consumers)

- **Persona:** Sitecore tenant editors or admins who encounter a paywalled Marketplace App after installation. They are not the blueprint's "users" in the product sense — but the reference app's UX copy and CTAs become the de-facto pattern adopters inherit.
- **What they encounter:** The four UX states (allowed, no-sub, seats-full, unassigned) and the post-gate welcome screen. Copy choices, tone, button labels.
- **Success for them:** The denial states feel warm, the next-step CTA is unambiguous, the post-gate welcome confirms "I have access."

## 5. Product Scope

### Architectural constraints (decisions, not deliverables)

- App-architecture: **client-side iframe (4a)** — no server-side API routes in PRD-000.
- App registration: **custom app** in Cloud Portal (Sitecore review not required for custom; public-Marketplace submission deferred to post-PRD-003).
- Single extension point: **SitecoreAI Full Screen** (`xmc:fullscreen`).
- Codebase shape: single Next.js app; reusable gate library lives at `src/lib/paywall/` inside the reference app (no monorepo, no separate npm package — see § 9 Codebase structure).

### In Scope (PRD-000) — deliverables

- Reference Marketplace App scaffold using `sitecore:setup-marketplace-client-side`.
- **`<PaywallGate>` React component** wrapper with hooks (`useEntitlement`, `usePurchaseUrl`) and a state machine that EVALUATES two states in PRD-000 and RENDERS four state components (per ADR-0011: tenant-only entitlement; per-user seat enforcement deferred):
  - **Evaluated by PRD-000 logic:**
    - `allowed` → renders children (tenant has active subscription)
    - `tenant_no_subscription` → "Start subscription" denial screen with placeholder CTA URL
  - **Design-reference components (rendered but unreachable in PRD-000 evaluation):** built, locked-copy-tested, and shipped in the blueprint so adopters see the full UX pattern. PRD-002 wires their evaluation logic.
    - `tenant_active_seats_full` → "All seats in use" screen with upgrade CTA (state never returned by PRD-000 evaluator)
    - `tenant_active_user_unassigned` → "Ask your team admin" screen (state never returned by PRD-000 evaluator)
- **Skeleton loading state** in the gated subtree during async entitlement fetch (prevents flash-of-allowed for unauthorized viewers).
- **Defensive layered render** for post-gate welcome (gracefully falls back when `application.context` fields are missing).
- **`EntitlementStore` TypeScript interface** + **`SupabaseStore` adapter** implementation. Two-table Supabase Postgres schema (`tenants`, `processed_events`) — `seats` table moves to PRD-002 per ADR-0011.
- **State-switcher CLI** (`pnpm seed:state <allowed|no-sub>`) for flipping between the two PRD-000-evaluated states. Two additional pseudo-commands (`seats-full`, `unassigned`) render the design-reference components directly without exercising the entitlement evaluator, so adopters can preview them via the smoke walkthrough.
- **Reference app single-page layout** with two visually distinct sections:
  - Top section: **free content** — minimal one-paragraph + one mock UI element. Renders for every viewer regardless of entitlement.
  - Bottom section: **gated content** — wrapped by `<PaywallGate>`. Post-gate welcome reads `application.context` for grounded "Welcome [user] for [tenant]" display.
- **Env-flag toggle** `NEXT_PUBLIC_PAYWALL_ENABLED`:
  - When `true` (enforced mode): gate evaluates entitlement; renders one of two live UX states (allowed / no-subscription) or skeleton during fetch.
  - When `false` (demo mode): gate renders children verbatim PLUS a persistent "Paywall disabled — demo mode" banner.
- **Dev override** `PAYWALL_DEV_OVERRIDE_USER_ID` env var — short-circuits the gate to `allowed` when the SDK-resolved user id matches.
- **Blok compliance from day one** — Blok tokens for colors, spacing, typography; WCAG AA accessibility.
- **OSS launch artifact:**
  - Public GitHub repo at `https://github.com/Chris1415/Sitecore.Plugin.PaywallBlueprint` (private during development; flipped to public visibility at ship moment).
  - README documenting the pattern, the two adapter interfaces, the env-flag, the swap-points, and an adoption guide (primary path = fork; secondary path = copy `src/lib/paywall/`).
  - CHANGELOG entry for PRD-000.
  - `docs/cold-read-notes.md` recording G3 cold-reader outcome.

### Out of Scope (PRD-000)

- Real provider integration (Stripe Checkout + webhook handler + Stripe Billing + Entitlements API + DB schema for subscriptions) → **PRD-001**.
- Webhook handling infrastructure — PRD-001 webhook MAY land as a Next.js API route in the reference scaffold (Stripe's webhook signature flow is iframe-agnostic; the scaffold migration from 4a to 4b is bounded and documented as PRD-001's decision per ADR-0005). Out-of-band hosting alternatives (Supabase Edge Function / separate Vercel project) remain open for PRD-001 to evaluate.
- **Per-user seat enforcement** (counting active seats, capacity check, auto-claim vs admin-assign policy, "unassigned" state evaluation, concurrent-seat-grab race-condition handling, `seats` table) → **PRD-002** per ADR-0011. The design-reference state components for seats-full and unassigned ship in PRD-000 as visual contracts; PRD-002 wires their evaluation logic.
- Seat-management UI (admin invite, revoke, "request seat" notification) → **PRD-002**.
- Standalone admin view → **PRD-002**.
- Dashboard Widget "Your plan" tile → **PRD-002+**.
- Customer self-service portal via Stripe Customer Portal → **PRD-003** (one Stripe API call; Stripe hosts the full UI).
- Trial mode (Stripe supports natively via `trial_period_days` on subscription create) → **PRD-001** design decision.
- Fail-open vs fail-closed policy when Stripe / store is unreachable → **PRD-001** design decision.
- Sitecore Marketplace public-app submission → **post-PRD-003 stretch**.
- Walkthrough video → **PRD-001** (real payment flow makes it valuable).
- Second provider adapter (Polar.sh, Lemon Squeezy, Paddle) → **post-PRD-003** (Stripe must stabilize first).
- A real "feature" behind the gate (Premium Page Stats, Bulk Rename, etc.) — **never**. The blueprint is intentionally feature-free; adopters bring their own feature.
- i18n / localization — **deferred**. PRD-000 ships English copy only.

### Explicitly out (clarifications)

- The blueprint does **not** prescribe a hosting provider for adopters. The reference app deploys to Vercel; adopters can deploy anywhere Next.js runs.
- The blueprint does **not** prescribe a payment provider. **Stripe direct** is the v1 worked example (operator OK with merchant-of-record posture; Stripe Tax is an optional layer for global VAT); the `PaymentProvider` adapter interface is the contract.
- The blueprint does **not** prescribe an entitlement-store backend. Supabase Postgres is the v1 worked example; the `EntitlementStore` adapter interface is the contract.
- The blueprint **client gate is UX, not security**. Per `storage/paywall-providers-research-2026-05-13.md` § 8.5: a determined user can force `<PaywallGate>` to render premium UI via devtools. Real security for paid features lives in *server-side* API-route guards (PRD-001 ships a `withEntitlement(handler)` HOF for that). PRD-000 has no premium feature behind the gate, so this is purely an adopter-documentation concern — call it out clearly in the README.

## 6. User Stories

### US-1 — Allowed user sees premium content

**As an** editor on a tenant with an active subscription and an assigned seat,
**when I** open the Paywall Blueprint app from Cloud Portal,
**I want** to see the premium content with my name and my tenant's name displayed,
**so that** I have confirmation that the gate recognized me and granted access.

**Acceptance criteria:**

- The free section at the top of the page renders within 1 second of app mount.
- The gated section initially renders a skeleton placeholder for up to 2 seconds while entitlement is fetched.
- Once the entitlement resolves to `allowed`, the skeleton is replaced with the post-gate welcome screen.
- The welcome screen renders user + tenant identity per the defensive layered render priority chain defined in FR-9 (single source of truth — AC does not re-spec the chain to avoid divergence).
- No banner is shown when `NEXT_PUBLIC_PAYWALL_ENABLED=true`.

### US-2 — Tenant has no subscription

**As an** editor on a tenant that does not have an active subscription,
**when I** open the Paywall Blueprint app,
**I want** to see a clear denial screen explaining the situation and offering a next step,
**so that** I know how to get access (or who to ask).

**Acceptance criteria:**

- The gated section resolves to the `tenant_no_subscription` UX state within 2 seconds of mount.
- The state shows a heading conveying "Start subscription" intent (final copy from UI designer).
- The state shows a primary CTA labeled per the copy quality bar (warm, no shame, clear next step) that links to the provider's checkout URL. In PRD-000 this is a placeholder URL (e.g., `https://example.com/buy`); PRD-001 replaces it with a real Lemon Squeezy URL.
- The free section above continues to render normally and is visible above the denial screen on the same page.

### US-3 — Tenant subscribed, user has no seat

**As an** adopter forking the blueprint who needs to wire up per-user seat enforcement,
**when I** look at the blueprint's state component library,
**I want** the `UserUnassignedState` component shipped as a design-reference with locked copy + accessibility contract intact,
**so that** PRD-002 (or my own seat-enforcement code) can wire it into the gate evaluator without re-designing the screen.

**Acceptance criteria (PRD-000 — design-reference component only):**

- `UserUnassignedState` component exists at `src/lib/paywall/states/UserUnassignedState.tsx`.
- The component renders the locked copy: heading "Ask your team admin", body "Your tenant has a plan, but your team admin hasn't given you a seat yet." No CTA in PRD-000 (deliberate — disabled-button UI signals broken pattern). The "Request seat" notification flow lands in PRD-002.
- Locked-copy regression tests assert the strings verbatim (per § 10 test contract).
- The component is verifiable via the design-reference walkthrough (`pnpm seed:state unassigned` renders the component directly without exercising the entitlement evaluator).
- **PRD-000 evaluation logic NEVER returns `tenant_active_user_unassigned`** — the state is unreachable from the gate's resolver. PRD-002 adds the seat-count branch that produces this state.

### US-4 — Design-reference for seats-full UX (PRD-000 component only; evaluation deferred to PRD-002)

**As an** adopter forking the blueprint who needs to wire up seat-capacity enforcement,
**when I** look at the blueprint's state component library,
**I want** the `SeatsFullState` component shipped as a design-reference with locked copy + CTA pattern intact,
**so that** PRD-002 (or my own seat-counting code) can wire it into the gate evaluator without re-designing the screen.

**Acceptance criteria (PRD-000 — design-reference component only):**

- `SeatsFullState` component exists at `src/lib/paywall/states/SeatsFullState.tsx`.
- The component renders the locked copy: heading "All seats in use", counter "{seatsTotal} of {seatsTotal} seats in use" (component accepts `seatsTotal` prop), body "Ask your team admin to reassign a seat, or upgrade your plan for more.", CTA "Upgrade plan" (placeholder URL in PRD-000; PRD-001 wires real Stripe URL).
- Locked-copy regression tests assert the strings verbatim.
- The component is verifiable via the design-reference walkthrough (`pnpm seed:state seats-full` renders the component directly without exercising the entitlement evaluator).
- **PRD-000 evaluation logic NEVER returns `tenant_active_seats_full`** — the state is unreachable from the gate's resolver. PRD-002 adds the seat-count branch that produces this state.

### US-5 — Free section visible regardless of entitlement state

**As any** editor (paid or unpaid, with seat or without),
**when I** open the Paywall Blueprint app,
**I want** to see the free section content at the top of the page immediately,
**so that** I always get *some* value from the app and the freemium-within-app pattern is visible to me.

**Acceptance criteria:**

- The free section renders within 1 second of app mount regardless of entitlement-store fetch state.
- The free section does not depend on `application.context` resolution (it renders with anonymous content suitable for any viewer).
- The free section is visually distinct from the gated section (clear separator, distinct heading hierarchy).
- The free section content is intentionally minimal: one paragraph of explanatory text + one mock UI element (e.g., a tile or button) demonstrating "this is where free features go."

### US-6 — Adopter disables paywall via env-flag

**As an** adopter of the blueprint working locally on a fork (or running a self-hosted "everything-free" deployment),
**when I** set `NEXT_PUBLIC_PAYWALL_ENABLED=false`,
**I want** the gate to render the gated content verbatim and visibly signal that the paywall is disabled,
**so that** I can iterate on the gated content without entitlement-store roundtrips and so that the OSS-fork story is honest about which mode is active.

**Acceptance criteria:**

- When `NEXT_PUBLIC_PAYWALL_ENABLED=false`, the `<PaywallGate>` renders children verbatim with no entitlement-store call.
- A persistent banner labeled "Paywall disabled — demo mode" is visible on the page when the flag is off. Banner styling decisions land at `/architect`.
- Toggling the flag at compile time (re-deploy) visibly removes / adds the banner and changes gate behavior.
- The dev override `PAYWALL_DEV_OVERRIDE_USER_ID` has no effect when `NEXT_PUBLIC_PAYWALL_ENABLED=false` (the flag takes precedence — flag-off bypasses the gate entirely).

### US-7 — Local developer bypasses gate via dev override

**As a** local developer running the reference app or an adopter fork on `localhost`,
**when I** set `PAYWALL_DEV_OVERRIDE_USER_ID=<my-user-id>`,
**I want** the gate to short-circuit to `allowed` when the SDK-resolved user id matches my override value,
**so that** I can iterate on the gated content without manually seeding entitlements.

**Acceptance criteria:**

- When `PAYWALL_DEV_OVERRIDE_USER_ID` is set AND `NEXT_PUBLIC_PAYWALL_ENABLED=true`, the gate skips the entitlement-store call IF the SDK-resolved user id matches.
- When the override does not match the SDK user id (e.g., a different teammate is testing), the gate runs the entitlement-store check normally.
- The dev override is documented in the README under a "Local development" section.
- The dev override is intentionally an environment variable (not a UI toggle) to keep it explicit and out of production state.

## 7. Functional Requirements

### FR-1 — `<PaywallGate>` component contract

The library MUST export a `<PaywallGate>` React component (functional, hooks-based) with the following contract:

- **Props:** `children` (ReactNode); optional `onStateChange` callback receiving the resolved entitlement state.
- **Behavior on mount:**
  1. Read `NEXT_PUBLIC_PAYWALL_ENABLED`. If `false`, render `children` + the demo-mode banner (rendered via a sibling component or portal — banner placement is a UI concern).
  2. **Wait for the Marketplace SDK provider's `context-ready` signal.** Render the skeleton in the gated subtree position until both context is ready AND the entitlement promise resolves. (The exact ready-signal mechanism — promise resolution, hook state flag, or `application.context` non-null — is locked in `/architect` after the DQ6 probe captures actual SDK behavior. This step replaces the earlier "synchronous read" framing to align with R6 mitigation.)
  3. Once context is ready, read `application.context`. Validate that `tenantId` and `userId` are non-empty strings; if either is missing/malformed, fall through to the top-level React error boundary (see NFR-6).
  4. Read `PAYWALL_DEV_OVERRIDE_USER_ID`. If set AND matches `context.user.id`, short-circuit to `allowed` without calling the entitlement store.
  5. Otherwise call `EntitlementStore.getEntitlement(tenantId, userId)`. While the promise is pending, continue rendering the skeleton.
  6. On resolution, render the matching UX state (one of the four discriminated-union variants in FR-2).
- **Error handling:** If the entitlement-store call rejects OR `application.context` is malformed, the component throws — the surrounding top-level error boundary (NFR-6) renders a generic "Something went wrong — please try again" message. PRD-000 deliberately does not implement an in-gate retry UX; PRD-001 will introduce explicit provider-unreachable UX with retry logic.

### FR-2 — `EntitlementStore` interface contract (split: runtime vs seed)

The library MUST export **two separate TypeScript interfaces** to avoid contract pollution. Adopters who swap the store in production only need to implement the runtime interface.

**`EntitlementStore` (production-runtime contract — required for adopters):**

- `getEntitlement(tenantId: string, userId: string): Promise<EntitlementResult>`

**`EntitlementSeed` (dev / CLI contract — optional for adopters; required for the state-switcher CLI):**

- `seedTenant(args: { tenantId: string, plan: string, seatsTotal: number, status: 'active' | 'cancelled' | 'past_due' }): Promise<void>`
- `seedSeat(args: { tenantId: string, userId: string }): Promise<void>`
- `clearState(): Promise<void>`

The blueprint's `SupabaseStore` class MUST implement both interfaces. Adopters who fork and swap stores only need their replacement class to implement `EntitlementStore`; the seed interface can be skipped entirely (their dev / smoke flow may differ).

**`EntitlementResult` discriminated union** — the TYPE declares four variants (so PRD-002 can extend the evaluator without changing the interface shape), but the **PRD-000 `SupabaseStore.getEntitlement` implementation only ever returns two of them** per ADR-0011 (tenant-only entitlement):

```typescript
type EntitlementResult =
  | { status: 'allowed' }
  | { status: 'tenant_no_subscription' }
  // Below: declared in the type for forward-compatibility with PRD-002.
  // PRD-000 SupabaseStore NEVER returns these; design-reference components only.
  | { status: 'tenant_active_seats_full'; seatsTotal: number }
  | { status: 'tenant_active_user_unassigned' };
```

**Evaluation rule (PRD-000):** `SupabaseStore.getEntitlement(tenantId, userId)` MUST short-circuit on the `tenants` row only:

```text
SELECT * FROM tenants WHERE tenant_id = :tenantId
  → no row OR status != 'active' → return { status: 'tenant_no_subscription' }
  → row found AND status = 'active' → return { status: 'allowed' }
```

The `userId` parameter is ACCEPTED but NOT CONSULTED in PRD-000. PRD-002's evaluator extends this query with the `seats` table join and adds the two seat-related branches.

**Note on `seatNumber`:** Removed from the `allowed` variant in PRD-000. PRD-002's seat-management UI may re-add it.

PRD-001 will extend `EntitlementStore` with `recordPurchaseEvent` for Stripe webhook idempotency (against the `processed_events` table — see § 10).

### FR-3 — `SupabaseStore` adapter implementation

The library MUST provide a concrete `SupabaseStore` class implementing **both** the `EntitlementStore` and `EntitlementSeed` interfaces against a Supabase Postgres database.

**Schema:** The authoritative schema definition lives in § 10 Data Model Overview (single source of truth). The `SupabaseStore` queries MUST match that schema exactly. Earlier divergence between FR-3 and § 10 (`last_seen_at`, `created_at`, `updated_at`) is resolved by deferring to § 10.

### FR-4 — Reference app single-page freemium layout

The reference Marketplace App MUST render a single page (no client-side routing) with two visually distinct sections:

- **Top section (free):** A minimal informational area — one paragraph of copy + one mock UI element. No SDK reads. Always visible. Marked clearly as "free" or equivalent.
- **Bottom section (gated):** Wrapped by `<PaywallGate>`. Contains the post-gate welcome screen (allowed state) or one of three denial screens. Marked clearly as "premium" or equivalent.

Both sections MUST be visible on a single screen without scrolling on a standard SitecoreAI Full Screen viewport (≥ 1024×768 effective viewport inside the iframe).

### FR-5 — Four UX state components (2 live + 2 design-reference)

Each of the four state outcomes (`allowed`, `tenant_no_subscription`, `tenant_active_seats_full`, `tenant_active_user_unassigned`) MUST be rendered by a distinct, identifiable component shipped in `src/lib/paywall/states/`. PRD-000 evaluator only routes to the first two (per ADR-0011 + FR-2); the other two ship as design-reference components verifiable via `pnpm seed:state seats-full | unassigned`. Each component MUST:

- Use Blok-compliant tokens for colors, spacing, and typography.
- Meet WCAG AA contrast ratios.
- Be keyboard-navigable (focus order, visible focus rings).
- Render copy that satisfies the quality bar set in § 11 and the locked strings finalized in UI design § 4.

The design-reference components (`SeatsFullState`, `UserUnassignedState`) have the same testing and accessibility contract as the live components — locked-copy regression tests, axe-core scans, keyboard nav, and contrast assertions all apply. This ensures PRD-002 inherits the components fully baked.

### FR-6 — Env-flag toggle + demo-mode banner

The library MUST honor `NEXT_PUBLIC_PAYWALL_ENABLED`:

- `=true` (or unset — default is enforced): gate evaluates normally.
- `=false`: gate is a fragment pass-through; a persistent banner displays the "Paywall disabled — demo mode" message.

The banner MUST be visible on the page when the flag is off and absent when it is on. Banner placement, exact wording (within the quality bar), and styling are UI design concerns (`/architect`).

### FR-7 — Dev override env var

The library MUST honor `PAYWALL_DEV_OVERRIDE_USER_ID`. When set AND the SDK-resolved `context.user.id` matches the override value AND `NEXT_PUBLIC_PAYWALL_ENABLED=true` AND `NODE_ENV !== 'production'` (compile-time check per NFR-5), the gate short-circuits to `allowed` without calling the entitlement store.

The override MUST be intentionally narrow: only matching the named user id, not "anyone on localhost." This keeps the override explicit and prevents accidental disclosure in shared dev environments. The compile-time `NODE_ENV` guard ensures the override branch is dead-code-eliminated from production bundles.

### FR-8 — State-switcher CLI

The repo MUST ship a CLI command (`pnpm seed:state <state>`) that flips the Supabase entitlement state to one of: `allowed`, `no-sub`, `seats-full`, `unassigned`. **CLI is the committed approach** — no JSON-seed alternative. Rationale: the smoke walkthrough needs to flip states quickly across screenshots, and one command per state is cleaner in the README than "edit this JSON then restart." Operator chose CLI in /create-prd DQ1 with the JSON option held as alternative; the alternative is now closed.

The CLI MUST:

- Read tenant/user identifiers from a local `.env.local` (operator's tenant + operator's user id, captured during the DQ6 probe).
- Implement against the `EntitlementSeed` interface (so the same CLI works against any seed-capable adapter).
- Idempotently apply the target state (truncate + reseed, OR upsert — implementation detail in `/architect`).
- Print a clear success message naming the state applied (e.g., "✓ State applied: seats-full (tenant=foo, seats_total=5, current_seats=5)").

### FR-9 — Post-gate context-grounded welcome

When the gate resolves to `allowed`, the rendered post-gate component MUST display the user's identity and tenant's identity, sourced from `application.context`. The component MUST implement defensive layered rendering:

- Display priority (user): `context.user.name` → `context.user.email` → `"there"` (generic fallback).
- Display priority (tenant): `context.tenant.name` → truncated `context.tenant.id` (last 8 chars) → `"your tenant"` (generic fallback).

The exact field names depend on the captured `application.context` shape from the DQ6 probe — final field accessors are confirmed during `/architect`.

## 8. Non-Functional Requirements

### NFR-1 — Performance

- **Gate evaluation latency:** From mount to resolved state, gate MUST complete within 2 seconds on a tenant in the same region as the Supabase instance. The free section MUST render within 1 second regardless of entitlement-store fetch progress.
- **Skeleton display threshold:** If entitlement resolution takes longer than 100ms, the skeleton MUST be visible (no flash of empty content).
- **First contentful paint:** ≤ 2 seconds on the SitecoreAI Full Screen extension point, cold mount.

### NFR-2 — Accessibility

- **WCAG AA compliance** across all four UX states and the free section.
- All interactive elements (CTAs, buttons) keyboard-navigable with visible focus rings.
- All non-text content (mock UI elements, icons) have appropriate text alternatives.
- Color contrast verified against Blok tokens at runtime.

### NFR-3 — Browser support

- Targets the browsers supported by Sitecore Cloud Portal (the parent iframe environment): current Chromium-based browsers (Chrome, Edge), current Firefox, current Safari.
- No support for IE11 or other legacy browsers.

### NFR-4 — Internationalization

- **English only** for PRD-000. i18n support is deferred — all copy lives in untranslated literals.
- The code structure MUST not actively *prevent* future i18n (e.g., copy lives in a single module that could later be wrapped by a translator).

### NFR-5 — Security

- No secrets in client-side bundle. `NEXT_PUBLIC_*` env vars are limited to the env-flag (which is non-sensitive). `PAYWALL_DEV_OVERRIDE_USER_ID` is **NOT** prefixed `NEXT_PUBLIC_` — it is server-side only and Next.js tree-shakes it out of client bundles in production builds.
- The dev override MUST be guarded at compile time, not runtime: the gate's override-check expression MUST be wrapped in `if (process.env.NODE_ENV !== 'production' && process.env.PAYWALL_DEV_OVERRIDE_USER_ID)` so that production builds dead-code-eliminate the entire branch via Next.js / Webpack tree-shaking. Runtime-only guards are insufficient — a misconfigured production build with `NODE_ENV` unset would still leak the override.
- Supabase service-role keys MUST live server-side only (in CI scripts and the state-switcher CLI). Anonymous keys (with row-level security) are used in any client-side context if applicable.

### NFR-6 — Reliability + error boundary

- The reference app MUST wrap `<PaywallGate>` (and the rest of the gated subtree) in a **top-level React error boundary** that renders a generic "Something went wrong — please try again" fallback. The error boundary MUST log the thrown error to `console.error` with a stable prefix (`[PaywallBlueprint]`) so adopters can grep for it in browser DevTools / Vercel logs / Sentry.
- When `<PaywallGate>` encounters a malformed `application.context` (missing `tenantId` or `userId`), it MUST throw — the error boundary catches it and renders the fallback. This is intentional: the gate does not invent UX for "context is broken"; that's a host-page integration error worth surfacing visibly.
- When `EntitlementStore.getEntitlement()` rejects, the rejected promise propagates as a thrown error to the boundary. PRD-000 does not implement in-gate retry; PRD-001 will introduce explicit provider-unreachable UX with retry logic.
- The free section (above the gate) MUST render normally even when the gated subtree falls into the error boundary. Adopters should never see a fully-blank page because of a gate failure.

### NFR-7 — Code quality

- TypeScript strict mode enabled (`strict: true` in tsconfig).
- No `any` types in the public API of the library (`<PaywallGate>`, `EntitlementStore`, adapter implementations).
- Code coverage for the gate logic: ≥ 80% for unit tests on the gate's state-resolution decision tree.

## 9. Technical Considerations

### Codebase structure (locked at this PRD; not deferred to architect)

- **Single Next.js application.** No monorepo, no separate npm package, no `packages/*` workspace structure.
- **Reusable gate library** lives at `src/lib/paywall/` inside the reference app, with internal structure:
  - `src/lib/paywall/PaywallGate.tsx` — the gate component (FR-1)
  - `src/lib/paywall/types.ts` — `EntitlementStore`, `EntitlementSeed`, `EntitlementResult` (FR-2)
  - `src/lib/paywall/stores/SupabaseStore.ts` — concrete adapter (FR-3)
  - `src/lib/paywall/stores/index.ts` — adapter re-exports
  - `src/lib/paywall/hooks/` — `useEntitlement`, `useSeat`, `usePurchaseUrl`
  - `src/lib/paywall/states/` — the four UX state components (FR-5)
  - `src/lib/paywall/DemoModeBanner.tsx` — env-flag banner (FR-6)
- **State-switcher CLI** lives at `scripts/seed-state.ts` (run via `pnpm seed:state <state>`).
- **Reference app pages** at `src/app/*` consume `src/lib/paywall/` as if it were an external library — no cross-imports from `src/lib/paywall/` into `src/app/*`. This enforces the "library is portable" contract.
- **Why no monorepo / no separate package:** Optimizes for the primary adoption path (fork the whole repo). A separate `paywall-blueprint-core` package would require adopters to also publish or vendor it, adding complexity to the 1-hour quickstart goal. Post-PRD-003 stretch: extract to npm package if external demand emerges. Until then, the `src/lib/paywall/` folder boundary is the unit of reuse.

### Stack and architecture

- **Frontend:** Next.js (App Router) + TypeScript.
- **UI:** Blok-compliant via shadcn/ui scaffold with Blok tokens (per `sitecore:blok-setup` skill).
- **Marketplace SDK:** `@sitecore-marketplace-sdk/client` for `application.context` read.
- **App architecture:** **4a client-side iframe** (per DQ4). The reference app has no server-side API routes in PRD-000. All SDK interactions go through the postMessage bridge to the portal.
- **App registration:** **Custom app** in Cloud Portal. Public Marketplace submission deferred to post-PRD-003 stretch.
- **Backing store:** Supabase Postgres (free tier sufficient for PRD-000–PRD-002 volumes per A5).
- **Hosting:** Vercel (per A4).
- **State-switcher:** Standalone CLI script (Node.js + `@supabase/supabase-js`) that runs locally for smoke testing. Uses the service-role key from `.env.local`.

### Two first-class abstraction boundaries (already captured as ADRs)

The blueprint's core architectural commitment is that **two specific surfaces are swappable** without touching the rest of the codebase:

1. **`PaymentProvider` adapter** (ADR-0003) — generate Checkout URL, verify webhook signature, parse webhook payload to entitlement change. **v1 implementation: Stripe direct** (Stripe Billing + Entitlements API + Customer Portal). Interface defined in PRD-000 as type-only placeholder; concrete adapter lands in PRD-001.
2. **`EntitlementStore` adapter** (ADR-0002) — `getEntitlement` (runtime) + seed methods (CLI). v1 implementation: `SupabaseStore` against Supabase Postgres free tier.

A third design pattern is recorded as ADR-0004:

3. **Env-flag toggle with signaled pass-through** — banner-on-disabled rather than silent bypass.

A fourth, added in this revision: **ADR-0011 — Tenant-only entitlement in PRD-000; per-user seat enforcement deferred to PRD-002.**

### Stripe integration shape (forward-looking note for PRD-001)

PRD-001's Stripe wiring is documented in detail at `storage/paywall-providers-research-2026-05-13.md` § 8.1–8.6. Headline shape:

- **Five components** in PRD-001: `<PaywallGate>` (already in PRD-000), `useEntitlement` hook, Next.js API routes (`/api/entitlement`, `/api/checkout`, `/api/portal`, `/api/webhooks/stripe`), Supabase DB (PRD-000 ships `tenants` + `processed_events`; PRD-002 adds `seats`), Stripe config (one Product + one or more Prices + one Feature for Entitlements API + one Customer Portal config + one webhook endpoint).
- **Webhook events** (PRD-001 wires): `customer.subscription.created/updated/deleted`, `invoice.payment_failed/succeeded`, optionally `entitlements.active_entitlement_summary.updated`. All idempotent via `processed_events.event_id` unique-on-conflict.
- **Signature verification mandatory** on every webhook call via `stripe.webhooks.constructEvent(rawBody, sig, endpointSecret)`.
- **Checkout flow** opens in a new tab (`window.open`) since Stripe Checkout cannot be iframed reliably. Return-flow uses `window.opener.postMessage('paywall:refresh', origin)` from a success page on the app's own domain, with a 30-second poll fallback if iframe sandbox blocks `postMessage` (open SDK question — see § 14).

### Webhook hosting (PRD-001 architecture decision)

Because PRD-000 scaffolds 4a client-side (no server routes), PRD-001 has three viable hosting options for the Stripe webhook handler — to be decided in PRD-001 architecture (one ADR):

- **(a) Next.js API route in the reference app** (scaffold migration 4a → 4b — bounded effort per ADR-0005 R8). Most idiomatic for a Next.js Marketplace app; Stripe's signature flow is iframe-agnostic.
- **(b) Supabase Edge Function** — runs in the same project as the entitlement store; minimal latency.
- **(c) Separate Vercel project (`paywall-blueprint-webhooks`)** — independent deployment; isolates webhook from iframe app.

PRD-001 architecture stage picks one and writes a dedicated ADR.

### DQ6 probe — operator action

Before `/architect` finalizes the post-gate welcome design, the operator runs a 5-minute probe against an existing Sitecore tenant to capture the actual `application.context` shape. The probe captures:

- The full JSON shape of `context.user` (which fields are present, types).
- The full JSON shape of `context.tenant` (or whatever the field name actually is).
- A screenshot of the captured payload.

The captured shape lands in `project-planning/architecture/sdk-fixtures/application-context.json` and `/architect` references it when finalizing FR-9's field accessors.

### Cloud Portal registration values (drives the post-scaffold step)

| Field | Value |
|-------|-------|
| App name | `Paywall Blueprint (Test)` for dev; `Paywall Blueprint` for ship |
| App URL (dev) | `https://localhost:3000` |
| App URL (prod) | TBD at deploy time (Vercel-assigned) |
| Extension points | `xmc:fullscreen` only |
| Route URL | `/` (single page; resolved by `sitecore:marketplace-sdk-extension-routes` skill after scaffold) |
| API access | Minimal — `application.context` read only. No XMC scopes needed in PRD-000. |
| Authorization type | Portal-brokered (default for 4a) |

## 10. Data Model Overview

### Supabase Postgres schema (PRD-000 surface — 2 tables only per ADR-0011)

```sql
CREATE TABLE tenants (
  tenant_id TEXT PRIMARY KEY,
  stripe_customer_id TEXT NULL,            -- populated by PRD-001 webhook; NULL in PRD-000
  subscription_id TEXT NULL,               -- populated by PRD-001 webhook; NULL in PRD-000
  plan TEXT NOT NULL DEFAULT 'starter',
  seats_total INTEGER NOT NULL DEFAULT 1,  -- PRD-002 consumes; PRD-000 ignores
  status TEXT NOT NULL CHECK (status IN ('active', 'cancelled', 'past_due')),
  period_end TIMESTAMPTZ NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE processed_events (
  event_id TEXT PRIMARY KEY,               -- Stripe event.id; unique-on-conflict for webhook idempotency
  processed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**PRD-000 usage:**
- `tenants` populated via state-switcher CLI (`pnpm seed:state <allowed|no-sub>`). `seats_total` defaults to `1` and is unused by PRD-000 evaluator.
- `processed_events` table exists but is empty in PRD-000 (provisioned for PRD-001's Stripe webhook idempotency per research doc § 8.3).
- **`seats` table is NOT in PRD-000.** PRD-002 adds the table, the seat-count branches in the evaluator, and the seat-management UI.

**Entitlement evaluation logic — PRD-000** (single tenant-row lookup; no seats logic per ADR-0011):

```typescript
async function getEntitlement(tenantId: string, _userId: string): Promise<EntitlementResult> {
  // _userId is accepted by the signature for interface stability across PRDs,
  // but PRD-000 does NOT consult it. PRD-002 extends with the seats branch.
  const { data: tenant } = await supabase
    .from('tenants')
    .select('*')
    .eq('tenant_id', tenantId)
    .maybeSingle();

  if (!tenant || tenant.status !== 'active') {
    return { status: 'tenant_no_subscription' };
  }
  return { status: 'allowed' };
}
```

**PRD-002 extension preview** (NOT in PRD-000 — included here only to show the seats-branch shape that ADR-0011 defers):

```typescript
// PRD-002 — extends with seats logic. Do NOT implement in PRD-000.
//   const seat = await supabase.from('seats')...;
//   if (seat) return { status: 'allowed', seatNumber, seatsTotal };
//   const count = await supabase.from('seats').select('count', ...).eq('tenant_id', tenantId);
//   if (count >= tenant.seats_total) return { status: 'tenant_active_seats_full', seatsTotal };
//   return { status: 'tenant_active_user_unassigned' };
```

## 11. UX Considerations

### Extension point: SitecoreAI Full Screen

The reference app uses **only** `xmc:fullscreen`. Single page, single route (`/`). Opens in its own browser tab from the Cloud Portal Apps section.

### Blok compliance

- All colors from Blok tokens (no hardcoded hex).
- All spacing from Blok scale (no magic numbers).
- All typography from Blok type ramps.
- WCAG AA contrast verified at runtime.
- See `sitecore:blok-setup` and `sitecore:blok-theming` skills.

### Copy quality bar (DQ5 — final wording from UI designer in `/architect`)

The four denial-state screens and the demo-mode banner inherit the following copy bar:

- **Warm, not punitive.** The denial isn't the editor's fault. Avoid "Access denied" / "You don't have permission" — those phrases shame the user. Prefer framings that suggest action: "Start your subscription," "Ask your admin," "Upgrade for more seats."
- **Clear next-step CTA.** Every denial state has exactly one primary action button with a verb-led label. No "Learn more" placeholders.
- **No scare-quotes around "free."** The freemium pattern is presented neutrally — free tier is a real product surface, not a teaser.
- **Concise.** Headline ≤ 6 words. Body ≤ 2 short sentences.
- **English only** for PRD-000.

The UI designer produces final wording as part of the `/architect` variant proposal. Adopters fork the polished copy as the inherited pattern.

### Skeleton loading state

While the entitlement promise is pending, the gated subtree renders a skeleton placeholder. The skeleton MUST occupy the visual space the resolved state would occupy (no layout shift). Skeleton styling uses Blok loading tokens.

### Demo-mode banner

When `NEXT_PUBLIC_PAYWALL_ENABLED=false`, a banner labeled "Paywall disabled — demo mode" (final copy from UI designer) is persistently visible. Placement, styling, and dismissibility are UI design concerns — but the banner MUST be:

- Visually distinct enough that an observer of a demo video would notice it.
- Not blocking the main content (placed at a page edge — top or bottom recommended).
- Non-dismissible (or dismissibility is session-only) so the signal remains honest.

### Post-gate welcome — defensive layered render

The post-gate welcome screen displays user + tenant identity using the priority chain in FR-9. The screen MUST always render *something* meaningful, even when display fields are absent — the gate's job is to confirm "you have access," and that confirmation cannot fall back to "Welcome `null`!"

## 12. Release Plan

PRD-000 ships in **five execution tranches** with an operator gate between each. Tranches are executed sequentially; each gate is a hard checkpoint against a real tenant before continuing.

### Tranche A — Scaffold + visual shell

**Delivers:**

- Run `sitecore:setup-marketplace-client-side` scaffold (canonical command).
- Register custom app in Cloud Portal with dev URL `https://localhost:3000` and `xmc:fullscreen` extension point.
- Configure Blok theming (`sitecore:blok-setup`).
- Build the single-page freemium layout — top free section, bottom premium section — with the gated subtree **hardcoded to render the "allowed" post-gate placeholder** (no real gate logic, no entitlement check, no `<PaywallGate>` component yet).

**Operator gate (Tranche A → B):**

- App installs on real Sitecore tenant.
- Page renders in the iframe at the SitecoreAI Full Screen extension point.
- Blok chrome looks correct (colors, spacing, typography match Blok variants).
- Free and premium sections are visible on one screen without scrolling.

### Tranche B — Store + `application.context` read

**Delivers:**

- Operator runs the 5-minute DQ6 probe; captured shape lands at `project-planning/architecture/sdk-fixtures/application-context.json`.
- Stand up Supabase project; create the 3-table schema; capture project URL + service-role key for `.env.local`.
- Implement `EntitlementStore` interface + `SupabaseStore` adapter.
- Implement state-switcher CLI (`pnpm seed:state <state>`).
- Wire the post-gate welcome screen to read `application.context` with defensive layered render (FR-9).

**Operator gate (Tranche B → C):**

- Seed an "allowed" entitlement via `pnpm seed:state allowed`.
- Install / refresh on real tenant.
- Post-gate welcome screen renders with the operator's actual user identity and tenant identity (sourced from `application.context`).

### Tranche C — `<PaywallGate>` + 4 UX states

**Delivers:**

- Build `<PaywallGate>` component (FR-1) with hooks, skeleton loading state (DQ3), and four UX state branches.
- Implement the four UX state components (FR-5): allowed (existing welcome), no-sub, seats-full, unassigned.
- Wire all four state components with quality-bar copy (DQ5 — designer final copy from `/architect`).
- Wrap the gated subtree in the top-level React error boundary (NFR-6).
- Placeholder CTA URLs in the denial states (`https://example.com/buy` or similar).

**Operator gate (Tranche C → D) — CHALLENGE GATE:**

This gate is **deliberately stricter than the others** — it stress-tests the paywall component before integration with env-flag and OSS surface.

- Walk through the **2 LIVE states** the evaluator returns: `pnpm seed:state allowed` and `pnpm seed:state no-sub`. Refresh the app on the real tenant after each seed; screenshot.
- Walk through the **2 DESIGN-REFERENCE states**: `pnpm seed:state seats-full` and `pnpm seed:state unassigned` render the components directly without invoking the entitlement evaluator. Screenshot each — these are the components PRD-002 will wire to the evaluator, and they must ship looking finished.
- Trigger the error boundary path once (e.g., temporarily break the Supabase connection string) and verify the fallback renders + the free section still shows.
- **Operator review (scored pass / soft-fail / hard-fail):**

| Question | Pass | Soft-fail (refine, no loop-back) | Hard-fail (loop back into Tranche C) |
|---------|------|------------------------------|--------------------------------------|
| Is the copy on the quality bar (warm, no shame, clear CTA) for all 4 state components? | All 4 pass | 1 state needs copy tweak | 2+ states off-bar |
| Is the skeleton clean (no flash-of-allowed, no layout shift)? | Yes | Minor jank | Visible flash-of-allowed |
| Are CTAs unambiguous in the 2 live states (allowed welcome, no-sub) and the 1 design-ref state with a CTA (seats-full)? | All 3 yes | 1 CTA ambiguous | 2+ CTAs ambiguous |
| Does the freemium pattern (free section above) work alongside each of the 4 rendered states? | All 4 yes | 1 layout collision | 2+ layout collisions |
| Does the error boundary catch + render fallback without blanking the free section? | Yes | — | No |
| Are the 2 design-reference states (seats-full, unassigned) visually finished (no "TODO copy" / no broken layout) so PRD-002 can wire them without redesign? | Yes | Minor polish missing | Components clearly unfinished |

- **All pass or soft-fail-only:** proceed to Tranche D. Soft-fail items get logged as `/code-review` follow-ups.
- **Any hard-fail:** loop back into Tranche C; refine; re-screenshot; re-review.

### Tranche D — Env-flag toggle + dev override

**Delivers:**

- Implement `NEXT_PUBLIC_PAYWALL_ENABLED` env-flag with demo-mode banner (FR-6).
- Implement `PAYWALL_DEV_OVERRIDE_USER_ID` dev override (FR-7), with production-mode guard.
- Document both in the README under "Configuration" and "Local development" sections.

**Operator gate (Tranche D → E):**

- Toggle `NEXT_PUBLIC_PAYWALL_ENABLED=false` and redeploy (or re-build for local).
- Banner appears; gate renders children verbatim regardless of entitlement state. **Screenshot the banner state** (required artifact for Tranche E README).
- Toggle back to `true`; banner disappears; gate evaluates entitlement again.
- Set `PAYWALL_DEV_OVERRIDE_USER_ID=<your-id>`; gate short-circuits to `allowed` even without a seeded entitlement.
- Production-build sanity check: `pnpm build` + grep the resulting `.next/` output for `PAYWALL_DEV_OVERRIDE_USER_ID` — should NOT appear (compile-time dead-code-elimination per NFR-5 worked).

### Tranche E — OSS launch surface

**Delivers:**

- Complete README at the repo root documenting:
  - The pattern (one-paragraph elevator pitch).
  - Quickstart (install, run, see the gate).
  - The two abstraction interfaces (`PaymentProvider`, `EntitlementStore`).
  - The env-flag and dev override.
  - The swap-points (how to plug in a different provider / store).
  - The four UX states and the freemium pattern.
  - Adoption guide ("how to use this in your own app").
- CHANGELOG entry for PRD-000.
- Smoke walkthrough doc at `docs/smoke-walkthrough.md` (or equivalent) with screenshots of each state.
- Final repo polish: license file, contribution guide stub, security policy (basic), code-of-conduct (optional).

**Operator gate (Tranche E → PRD-000 SHIPPED):**

- The README is readable cold by someone unfamiliar with the project.
- All four screenshots from Tranche C are embedded in the README or smoke-walkthrough doc.
- The CHANGELOG documents PRD-000 release.
- Repo is set to public visibility on GitHub.
- PR is opened (this branch `prd-000` → `main`).

### Tranche dependency / gates summary

| Tranche | Depends on | Operator gate | Real-tenant install required? |
|---------|------------|---------------|-------------------------------|
| A | — | Visual shell renders in iframe | Yes |
| B | A | Context-grounded welcome renders | Yes (re-install or refresh) |
| C | B | **Challenge gate** — all 4 states reviewed for quality | Yes |
| D | C | Env-flag toggles visibly; dev override works | Yes (or local) |
| E | D | OSS surface ready for public consumption | No (doc-only tranche) |

## 13. Risks and Mitigations

### R1 — `application.context` shape assumptions wrong (carried from brain-dump A1, A2)

**Risk:** If `application.context.tenantId` is not durable across reinstalls, or if user identity is not stable / not exposed in a useful form, the entitlement model breaks or the post-gate welcome degrades.

**Mitigation:** DQ6 probe runs in Tranche B before any gate-relevant logic is wired. Captured shape lands in `project-planning/architecture/sdk-fixtures/`. Code uses defensive layered render (FR-9) regardless — the gate always renders something sensible. If the probe reveals a hard structural issue, escalate to operator and consider SDK Sandbox detour (per session intent).

### R2 — Marketplace ToS prohibits external merchant-of-record (carried from A3)

**Risk (revised — operator OK with merchant-of-record posture):** With Stripe direct as the v1 provider, the operator IS the merchant of record by default. Self-handles VAT / sales-tax compliance unless Stripe Tax is layered in. If Sitecore's Marketplace ToS later requires a third-party MoR, the operator can flip to **Stripe Managed Payments** (public preview Feb 2026 — same Stripe account, same 5% + 50¢ MoR fees, one-config switch) without provider migration.

**Mitigation:** Operator explicitly accepts MoR posture in PRD-000 planning (see updated memory `project_paywall_blueprint_idea.md`). Stripe Tax decision can be deferred until the first non-US-tax-only adopter scenario emerges (OA-2 reframed). PRD-000 is unaffected — it has no real provider.

### R3 — PRD-000 scope creep from freemium pattern (carried from brain-dump R4)

**Risk:** Adding the freemium-within-app demo (D9) expanded PRD-000 scope. Could overrun the 1–2 day tranche-A-through-D estimate.

**Mitigation:** Keep the free section deliberately minimal (FR-4: one paragraph + one mock UI element). Resist adding any business logic to it during Tranche A. UI designer respects the "free section is minimal" constraint when producing the variant.

### R4 — Out-of-band webhook hosting decision deferred to PRD-001

**Risk:** Choice between Supabase Edge Function vs separate Vercel project for PRD-001's webhook handler isn't made yet. If the operator later finds neither works (auth issues, cold-start latency, signature-verification ergonomics), PRD-001 stalls.

**Mitigation:** PRD-001 architecture stage produces an ADR comparing both options on operational characteristics before committing. PRD-000 is unaffected — it has no webhook handler.

### R5 — Public OSS expectations escalate before v1 stabilizes

**Risk:** Once the repo is public (Tranche E), community may request second-provider adapter (Polar.sh / Lemon Squeezy / Paddle) before Stripe is even fully stable across all four PRDs.

**Mitigation:** README explicitly signals "v1 = Stripe direct; second provider after PRD-003 stabilizes." Set an informal issue-triage expectation in CONTRIBUTING.md. The `PaymentProvider` adapter abstraction means a community contributor CAN propose a Polar.sh adapter as a PR — but maintainer review priority stays with the Stripe path until PRD-003 ships.

### R6 — Iframe mount lifecycle race condition

**Risk:** If `application.context` is not ready on first render (SDK provider initializes asynchronously), gate behavior at the very first mount could be wrong (e.g., `userId` is undefined → entitlement call fails).

**Mitigation:** Architecture stage documents the SDK provider's context-readiness signal. Gate component waits for the provider to signal "context ready" before calling the entitlement store. Skeleton is shown until both context is ready AND entitlement resolves.

### R7 — Demo-mode banner visual interference with post-gate welcome

**Risk:** A persistent banner on top of a context-grounded "Welcome" screen could look cluttered or interfere with copy.

**Mitigation:** UI designer validates the banner + welcome layout together in the `/architect` variant. Banner placement is a UI concern, not a logic concern.

### R8 — DQ4 scaffold-choice consequence — PRD-001 webhook hosting decision (revised)

**Risk:** DQ4's choice of 4a client-side means PRD-001's Stripe webhook handler needs a hosting decision: add a Next.js API route to this scaffold (1-day migration 4a → 4b), or host out-of-band (Supabase Edge Function / separate Vercel project).

**Mitigation:** PRD-001 architecture stage produces an ADR choosing one of the three paths (see § 9 "Webhook hosting" subsection). All three are viable; PRD-001 evaluates against operational characteristics (cold-start latency, signature-verification ergonomics, deployment topology). PRD-000 is unaffected — it has no webhook handler.

## 14. Open Questions

Questions intentionally deferred — each maps to a specific later stage where it gets resolved.

### Resolvable in `/architect`

- **OQ-Arch-1** — DQ6 probe outcome: which `application.context` fields are exposed (user name? email? tenant name?). (Operator action OA-1.) Resolves brain-dump A1, A2.
- **OQ-Arch-2** — Demo-mode banner placement and styling. (Banner literal copy `"Paywall disabled — demo mode"` is locked in FR-6 / § 11 — only visual concerns are open.)
- **OQ-Arch-3** — Final copy for the four denial-state screens (UI designer in `/architect` variant — quality bar set in § 11; literal strings open).
- **OQ-Arch-4 — RESOLVED** in this PRD: seeding mechanism is the state-switcher CLI (FR-8). JSON-seed alternative closed.
- **OQ-Arch-5 (new — medium-priority parked from critical review):** Adopter Supabase setup complexity. The quickstart includes ~9 Supabase setup steps (create project, get keys, run schema migration, set env vars, …). What's the safest minimum "your quickstart works on first try" target — a copy-pasteable schema migration SQL block in the README, a `supabase init` automation, or a `pnpm setup:supabase` wizard? Architect picks based on tradeoffs.
- **OQ-Arch-6 (new — medium-priority parked from critical review):** Skeleton-state variation. Does each of the four UX states use the same generic skeleton, or do four distinct skeletons mirror each state's eventual layout? Architecture decision based on layout-shift risk.

### Resolvable in PRD-001

- **OQ-PRD001-1** — Stripe webhook hosting (3 options): Next.js API route in this scaffold (4a → 4b migration), Supabase Edge Function, or separate Vercel project (`paywall-blueprint-webhooks`)? (ADR in PRD-001 architecture; see § 9 "Webhook hosting".)
- **OQ-PRD001-2 — PARTIALLY RESOLVED:** PRD-000 commits to fail-closed-via-error-boundary for store-unreachable (FR-1, NFR-6). PRD-001 decides fail-open vs fail-closed for **Stripe webhook delivery delays** (e.g., webhook fires 30 s after Checkout success — does the gate poll, retry, or block?) — operator-configurable, defaults TBD.
- **OQ-PRD001-3 — RESOLVED in research:** Stripe supports trial mode natively via `trial_period_days` on subscription create (`stripe.subscriptions.create({ trial_period_days: 14, ... })`). No state-machine extension needed — `status: 'active'` covers trialing. PRD-001 wires the trial-period config as a checkout option.
- **OQ-PRD001-4 — RESOLVED:** Operator OK with merchant-of-record posture under Stripe direct (memory update 2026-05-13). Stripe Managed Payments (Feb 2026 public preview) is the upgrade path if MoR posture later flips. No legal-review blocker for PRD-001 kickoff.
- **OQ-PRD001-5 (new — from research § 8.7):** Does the iframe sandbox allow `window.opener.postMessage()` back from the Stripe Checkout success page (which lives on the app's own domain after redirect)? If not, the gate polls `/api/entitlement` every 3 s for 30 s after Checkout opens. Real-tenant probe required — could be answered by the SDK Sandbox app once it exists.

### Resolvable in PRD-002

- **OQ-PRD002-1** — Concurrent-seat-grab race handling (two users → 1 free seat → exactly one wins). Implementation contract.
- **OQ-PRD002-2** — "Request seat" notification flow (Marketplace SDK in-app notification? Email? Both?).
- **OQ-PRD002-3 (new):** Seat-numbering algorithm. If PRD-002 UI shows "you are seat 3 of 5," what's the algorithm — insertion order? `assigned_at` ordinal? Hash of `user_id`? PRD-000 deliberately omitted `seatNumber` from `EntitlementResult` to avoid pre-committing.

### Resolvable post-PRD-003

- **OQ-PostShip-1** — Marketplace public-app submission timing. After PRD-003 stabilizes? After first non-hahnsolo adopter signal?
- **OQ-PostShip-2** — Second extension point: Dashboard Widget "Your plan" tile candidacy.
- **OQ-PostShip-3 (new):** npm-package extraction. Extract `src/lib/paywall/` to a standalone `@hahnsolo/paywall-blueprint-core` package? Depends on external demand. PRD-000 deliberately defers (§ 9 Codebase structure).

### Deferred performance budgets (no current target)

- **OQ-NFR-1 (new — parked from critical review):** Bundle size budget for adopter ergonomics. A 5MB Next.js + shadcn baseline is more than adopters expect for a "fork and replace" reference. Set a budget (e.g., `next build` output ≤ 500 KB First Load JS) when first adopter signals friction.
- **OQ-NFR-2 (new — parked from critical review):** Gate-evaluation latency in cross-region deployments. Current 2s target (NFR-1) assumes Supabase same-region. Different-region case not specified; flag if a real adopter hits it.

### Operator action items (between PRD writing and `/architect`)

- **OA-1** — Run 5-minute `application.context` probe; capture shape JSON + screenshot; commit to `project-planning/architecture/sdk-fixtures/application-context.json`. Unblocks OQ-Arch-1. **Sole home for this action** (removed from § 5 In Scope to avoid double-listing).
- **OA-2 — REFRAMED:** Stripe Tax decision for global VAT compliance. **Optional**, not a PRD-001 blocker. Operator decides whether to enable Stripe Tax (one toggle in Stripe dashboard) before the first non-US-tax-only adopter scenario emerges. Lemon-Squeezy-as-MoR legal review is no longer applicable (provider revised).
- **OA-3 (pre-G3 ship gate):** Identify a named cold-reader for G3. Could be a colleague, a contractor, or an LLM agent in a clean context. Coordinate before Tranche E begins so the cold-read happens within Tranche E.

## 15. Future Opportunities

Out-of-scope items recorded so the product surface stays clear in conversation. These are NOT commitments.

- **Additional extension points** (PRD-002+): Dashboard Widget "Your plan: Pro · 3/5 seats used" tile; Standalone admin view for seat management.
- **Public Marketplace listing submission** (post-PRD-003 stretch): Blok review, accessibility audit, Sitecore approval. Distinct from public GitHub.
- **Second provider adapter** (post-PRD-003): Polar.sh, Lemon Squeezy, Paddle — each as a separate adapter file demonstrating the swap-point contract. The research at `storage/paywall-providers-research-2026-05-13.md` § 2 covers all five contenders considered.
- **Alternative entitlement stores**: Upstash KV, Firestore, self-hosted Postgres — each as a separate adapter.
- **Self-service customer portal** (PRD-003): Wraps **Stripe Customer Portal** (free, hosted; cancel / change plan / update card / view invoices — all hosted by Stripe). One `stripe.billingPortal.sessions.create({ customer, return_url })` call.
- **Multi-language support** (i18n): defer until first non-English adopter request.
- **Analytics integration**: which states do users hit most? Conversion from denial → purchase? Out of scope; adopters add their own.
- **Marketing surface**: a `hahnsolo.net/paywall-blueprint` landing page; a walkthrough video (PRD-001 candidate); a launch blog post.
- **Stripe Managed Payments upgrade** (post-PRD-003 stretch): If MoR posture later flips (Sitecore Marketplace ToS or operator preference), one-config switch to Stripe Managed Payments — same Stripe account, same fee structure (5% + 50¢), zero adapter code change.
- **Server-side entitlement enforcement helper** (PRD-001+): The research § 8.5 flags the client-gate-is-UX-not-security distinction. PRD-001 ships a `withEntitlement(handler)` HOF for Next.js API route handlers so adopters wrap their premium endpoints with one line.

---

## Handoff Metadata

- **Canonical run manifest:** `products/paywall-blueprint/project-planning/workflow/run-20260513T093404Z.json`
- **Source concept brief:** `products/paywall-blueprint/project-planning/brain-dumps/brain-dump-20260513T093404Z.md`
- **PRD input mode:** `combined` (brain-dump + /create-prd discovery DQ1-DQ6)
- **Recommended next command:** `/architect` — produces architecture blueprint + ADRs (PaymentProvider, EntitlementStore, env-flag pattern, scaffold choice, custom-app registration) + UI variant for the four denial states + demo-mode banner + post-gate welcome.
- **Recommended next input file:** `products/paywall-blueprint/project-planning/architecture/architecture-20260513T093404Z.md`
- **Branch:** `prd-000`
- **Operator action items before `/architect`:** OA-1 (5-min `application.context` probe — captures shape JSON to `project-planning/architecture/sdk-fixtures/application-context.json`).
- **Operator action items before PRD-001 kickoff:** OA-2 (Lemon Squeezy merchant-of-record legal review).
